const BASE_URL = "https://sinul.es";

const path = {
    SELECT_IMAGE: "/select-image",
    CUT_IMAGE: "/cut",
    PALETTE_CONFIGURATOR: "/palette",
    IMAGE_RESOLUTION: "/image-resolution",
    PATERN: "/patern",
};

export { path, BASE_URL };
